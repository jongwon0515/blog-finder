bootJar { enabled = false }
jar { enabled = true }

dependencies {
    implementation project(':common')
    implementation project(':connect')

    testImplementation project(':test')
    testImplementation project(':test').sourceSets.test.output
}
